{
  "scripts": {
    "test": "mocha test --opts mocha.opts",
    "test:auto": "mocha test --opts mocha.opts --watch"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "mocha": "^3.1.0",
    "mocha-tap-reporter": "^0.1.3",
    "sinon": "^1.17.6",
    "tsc" : "latest",
    "ts-node": "^1.3.0"
  }
}
